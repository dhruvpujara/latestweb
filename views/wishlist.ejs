
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Your Favourites</title>
    <link rel="stylesheet" href="/public/output.css">
</head>
<body class="bg-gradient-to-br from-gray-900 via-gray-800 to-black min-h-screen py-8 px-4">
    <!-- Header Section -->
    <h1 class="text-4xl font-bold text-center text-transparent bg-clip-text bg-gradient-to-r from-[#ff416c] to-[#ff4b2b] mb-12">
        Your Favourites...
    </h1>

    <!-- Favorites Container -->
    <div class="max-w-7xl mx-auto space-y-6">
        <% favorites.forEach(fav => { %>
            <!-- Anime Card -->
            <div class="bg-gray-800/50 backdrop-blur-sm rounded-2xl p-6 shadow-2xl border border-gray-700 hover:border-gray-600 transition-all duration-300
                        flex flex-col md:flex-row gap-6 items-center">
                
                <!-- Image Section -->
                <div class="w-full md:w-1/4 h-[300px] md:h-[200px]">
                    <img src="<%= fav.Image %>" 
                         alt="<%= fav.Name %>" 
                         class="w-full h-full object-cover rounded-xl shadow-lg">
                </div>

                <!-- Info Section -->
                <div class="flex-1 space-y-4">
                    <h2 class="text-2xl font-bold text-white/90 hover:text-white transition-colors">
                        <%= fav.Name %>
                    </h2>
                    
                    <div class="grid grid-cols-1 sm:grid-cols-2 gap-2 text-sm text-gray-300">
                        <p class="flex items-center gap-2">
                            <span class="text-[#ff416c]">Episodes:</span> 
                            <%= fav.episodes %>
                        </p>
                        <p class="flex items-center gap-2">
                            <span class="text-[#ff416c]">Genre:</span> 
                            <%= fav.genre %>
                        </p>
                        <p class="flex items-center gap-2">
                            <span class="text-[#ff416c]">Status:</span> 
                            <%= fav.status %>
                        </p>
                    </div>

                    <p class="text-gray-400 text-sm line-clamp-3 hover:line-clamp-none transition-all duration-300">
                        <%= fav.description %>
                    </p>
                </div>

                <!-- Action Button -->
                <button onclick="window.location.href='<%= fav.View %>'" 
                        class="px-6 py-3 bg-gradient-to-r from-[#ff416c] to-[#ff4b2b] rounded-xl text-white font-semibold
                               shadow-lg hover:shadow-xl transform hover:scale-105 transition-all duration-300">
                    Watch Now
                </button>
            </div>
        <% }); %>
    </div>
</body>
</html>