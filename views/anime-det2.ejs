<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= anime.Name %> - Details</title>
    <link rel="stylesheet" href="/public/output.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.7.2/css/all.min.css">
</head>

<body class="min-h-screen bg-gradient-to-br from-gray-900 via-gray-800 to-black py-8 px-4">
    <!-- Main Container -->
    <div class="max-w-6xl mx-auto bg-gray-800/50 backdrop-blur-sm rounded-2xl shadow-2xl border border-gray-700 overflow-hidden">
        <!-- Hero Section -->
        <div class="relative h-[400px] md:h-[500px] w-full">
            <img class="w-full h-full object-cover opacity-30" src="<%= anime.Image %>" alt="<%= anime.Name %> backdrop">
            <div class="absolute inset-0 bg-gradient-to-t from-gray-900 to-transparent"></div>
            
            <!-- Content Wrapper -->
            <div class="absolute bottom-0 left-0 p-8 w-full">
                <div class="flex flex-col md:flex-row gap-6 items-start md:items-end">
                    <!-- Poster Image -->
                    <img class="w-40 md:w-48 h-60 md:h-72 object-cover rounded-xl shadow-2xl border-2 border-[#ff416c] mx-auto md:mx-0" 
                         src="<%= anime.Image %>" alt="<%= anime.Name %>">
                    
                    <!-- Info Section -->
                    <div class="flex-1 text-center md:text-left">
                        <h1 class="text-3xl md:text-4xl font-bold text-white mb-4"><%= anime.Name %></h1>
                        
                        <!-- Metadata Grid -->
                        <div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-4 text-gray-300 text-sm">
                            <span class="flex items-center gap-2 justify-center md:justify-start">
                                <i class="fas fa-film text-[#ff416c]"></i>
                                <%= anime.episodes %> Episodes
                            </span>
                            <span class="flex items-center gap-2 justify-center md:justify-start">
                                <i class="fas fa-star text-[#ff416c]"></i>
                                <%= anime.genre %>
                            </span>
                            <span class="flex items-center gap-2 justify-center md:justify-start">
                                <i class="fas fa-circle-info text-[#ff416c]"></i>
                                <%= anime.status %>
                            </span>
                        </div>
                    </div>
                </div>
            </div>
        </div>

        <!-- Content Section -->
        <div class="p-8 space-y-6">
            <!-- Description -->
            <div class="text-gray-300 leading-relaxed">
                <%= anime.description %>
            </div>

            <!-- Actions -->
            <div class="flex flex-wrap gap-4 items-center justify-center pt-4">
                <form action="./liked" method="POST" class="w-full sm:w-auto">
                    <input type="hidden" name="animeId" value="<%= anime.Id %>">
                    <button class="w-full sm:w-auto px-6 py-3 bg-gradient-to-r from-[#ff416c] to-[#ff4b2b] rounded-xl 
                                 text-white font-semibold shadow-lg hover:shadow-xl transform hover:scale-105 
                                 transition-all duration-300 flex items-center justify-center gap-2">
                        <i class="fas fa-heart"></i>
                        Add to Favourites
                    </button>
                </form>
            </div>

            <!-- Watch Links -->
            <div class="border-t border-gray-700 pt-6">
                <h2 class="text-2xl font-bold text-transparent bg-clip-text bg-gradient-to-r from-[#ff416c] to-[#ff4b2b] mb-6 text-center">
                    Watch Links
                </h2>
                <div class="flex flex-wrap gap-4 justify-center">
                    <a href="<%= anime.View%>" 
                       class="px-6 py-3 bg-gradient-to-r from-blue-600 to-blue-800 rounded-xl text-white 
                              font-semibold shadow-lg hover:shadow-xl transform hover:scale-105 transition-all duration-300">
                        Hi Anime
                    </a>
                    <% if (anime['9ANIME']) { %>
                        <a href="<%= anime['9ANIME'] %>" 
                           class="px-6 py-3 bg-gradient-to-r from-purple-600 to-purple-800 rounded-xl text-white 
                                  font-semibold shadow-lg hover:shadow-xl transform hover:scale-105 transition-all duration-300">
                            9ANIME
                        </a>
                    <% } %>
                </div>
            </div>
        </div>
    </div>
</body>
</html>